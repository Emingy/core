import { createContext } from 'react';

import type { T{{ pascalCase name }}Context } from './types';

export const {{pascalCase name }}Context = createContext<T{{ pascalCase name }}Context | null>(null);
