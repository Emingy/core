import React from 'react';
import type { Meta } from 'storybook-react-rsbuild';

import { {{ pascalCase name}}, type T{{ pascalCase name }}Props } from './src';

const meta: Meta = {
    title: 'UI/{{ pascalCase name }}',
    component: {{ pascalCase name }},
};

export default meta;

export const Demo = (props: T{{ pascalCase name }}Props) => <{{ pascalCase name }} {...props} />;
