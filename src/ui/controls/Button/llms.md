# Button

| Prop              | Type                                  | Default     | Description |
| ----------------- | ------------------------------------- | ----------- | ----------- |
| `size`            | `"md" \| "sm" \| "lg"`                | `'md'`      | —           |
| `type`            | `"primary" \| "secondary" \| "alert"` | `'primary'` | —           |
| `variant`         | `"filled" \| "ghosted" \| "outlined"` | `'filled'`  | —           |
| `disabled`        | `boolean`                             | —           | —           |
| `htmlType`        | `"submit" \| "reset" \| "button"`     | —           | —           |
| `prefix`          | `ReactNode`                           | —           | —           |
| `postfix`         | `ReactNode`                           | —           | —           |
| `isLoading`       | `boolean`                             | `false`     | —           |
| `href`            | `string`                              | —           | —           |
| `navigateOptions` | `NavigateOptions`                     | —           | —           |
| `isFullWidth`     | `boolean`                             | —           | —           |
| `className`       | `string`                              | —           | —           |
| `icon`            | `TIcon`                               | —           | —           |
| `splitted`        | `boolean`                             | —           | —           |
| `dropdownContent` | `ReactNode`                           | —           | —           |
