# ToggleButton

| Prop          | Type                       | Default     | Description |
| ------------- | -------------------------- | ----------- | ----------- |
| `type`        | `"primary" \| "secondary"` | `'primary'` | —           |
| `className`   | `string`                   | —           | —           |
| `prefix`      | `ReactNode`                | —           | —           |
| `postfix`     | `ReactNode`                | —           | —           |
| `isLoading`   | `boolean`                  | —           | —           |
| `isFullWidth` | `boolean`                  | —           | —           |
