services:
    playwright:
        build:
            context: .
            dockerfile: Dockerfile.playwright
        volumes:
            - ./playwright:/app/playwright
            - ./test-results:/app/test-results
        ports:
            - '8080:3000'
